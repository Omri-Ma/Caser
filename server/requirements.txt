fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
sqlalchemy==2.0.36
alembic==1.14.0
pymysql==1.1.1
redis==5.2.1
cryptography==44.0.0
bcrypt==4.2.1
pyjwt==2.10.1
email-validator==2.2.0
structlog==24.4.0
python-multipart==0.0.20
reportlab==4.2.5
python-bidi==0.6.11
openpyxl==3.1.5
pytest==8.3.4
httpx==0.28.1
# Test-only: extracts real text from a generated narrative PDF to verify
# the itemized WorkLog table actually renders (a raw byte search doesn't
# work — the embedded Hebrew TTF is a subset font with a remapped glyph
# encoding, so literal ASCII text never appears in the content stream).
pymupdf==1.28.2

# /docs generation tooling (ERD, OpenAPI, Postman) is not needed by either
# app at runtime — see server/scripts/requirements-docs.txt instead.
