# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/

# Node
node_modules/
dist/
build/
.vite/

# Env / secrets
.env
.env.local
*.env.*.local

# Uploaded files (local disk storage backend)
server/storage/
server/uploads/
# Safety net for the now-fixed STORAGE_ROOT double-resolution bug (see
# docs/ai_usage.md) — nothing has ever been tracked here, but the pattern
# above doesn't cover this nested depth if a stale .env value ever
# resurfaces it.
server/server/

# Dev-only password-reset outbox (stand-in for real email — CLAUDE.md's
# Future additions), written by server/shared/dev_outbox.py
server/dev_outbox.jsonl

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Docker
docker-compose.override.yml

# Defense presentation (personal prep artifact, not part of the codebase)
docs/Caser_Defense_Presentation.pptx
